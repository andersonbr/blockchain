{
  "name": "eleicoes",
  "version": "1.0.0",
  "description": "eleicoes",
  "main": "main.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "sha256": "^0.2.0"
  }
}
